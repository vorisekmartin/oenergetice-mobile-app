// @flow
import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
  },
  textContainer: {
    flex: 1,
    alignItems: "center",
    alignSelf: "center",
  },
})
